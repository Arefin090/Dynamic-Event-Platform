module.exports = {
    testEnvironment: 'node', // Ideal for backend testing
    verbose: true, // Gives detailed test output
    setupFiles: ['dotenv/config'] // Ensures environment variables load for tests
  };
  